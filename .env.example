# RentFair Environment Variables

# Set NODE_ENV to 'production' in production environments
NODE_ENV=development

# Statistics Canada API - if you need API keys in the future
# STATCAN_API_KEY=your-api-key-here

# Data refresh interval in hours (default: 168 hours = 1 week)
DATA_REFRESH_INTERVAL=168

# Cache duration for API responses in seconds (default: 3600 = 1 hour)
API_CACHE_DURATION=3600

# Vercel deployment region (YYZ = Toronto) 
VERCEL_REGION=yyz1